# build.env contains your actual GCP project/bucket/region - never commit it.
# Distro recipe files (arch.env, debian.env, ...) hold no secrets and are
# tracked normally.
build.env

# LXC template tarballs are large binaries and shouldn't live in git.
*.tar.zst
*.tar
