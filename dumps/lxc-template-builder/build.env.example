# Copy this file to build.env and fill in real values:
#   cp build.env.example build.env
# build.env is gitignored so your project/bucket details never get committed.

# GCP project that will run the Cloud Build job
PROJECT_ID=your-gcp-project-id

# GCS bucket holding both the source template (under /base) and the
# finished template (uploaded under /custom). No trailing slash.
BUCKET=gs://your-bucket-name

# Filename of the existing template already uploaded to ${BUCKET}/base/
SRC_TEMPLATE=archlinux-base_20260420-1_amd64.tar.zst

# Space-separated pacman package list to install on top of the base template
PACKAGES=htop
